-- =============================================================
-- (폐기) user_num PK 전환 마이그레이션
--
-- 운영 DB에는 user_num 컬럼이 없고 players.user_id 가 PK이다.
-- 이 파일은 실행하지 말 것. 아무 작업도 하지 않는다.
-- =============================================================

SELECT 1;
