# MySQL ?????? ?? ??
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ergg_db?serverTimezone=Asia/Seoul
    username: ergg_user
    password: ergg_password
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  # JPA/Hibernate ??
  jpa:
    hibernate:
      ddl-auto: update  # ?? ????? 'update', ????? 'none' ?? 'validate'
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

# ?? ?? ??
server:
  port: 7071

# ?? ??
logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql: trace
    com.ergg: debug

# ??? ?????? ??
ergg:
  api:
    base-url: https://api.example.com/v1
    key: your-api-key
  cache:
    ttl: 3600  # ?? ?? ??(?)